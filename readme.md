#  Універсальний Медіа Завантажувач 📥

<!--![Project Banner](https://i.imgur.com/your-image-url.png) <!-- Можете створити та завантажити банер сюди -->

Потужний набір інструментів для завантаження медіа з популярних платформ. Проєкт включає в себе два незалежних компоненти:
1.  **Telegram Бот** — зручний інтерфейс для завантаження файлів через Telegram, з системою доступу для обраних користувачів.
2.  **Консольний Скрипт** — ультимативний завантажувач для локального використання без жодних обмежень на розмір файлів.

---

## 🛠️ Встановлення та Налаштування

**1. Клонуйте репозиторій:**```bash
git clone https://github.com/Btema2/telegram-yt-downloader.git
```
```bash
cd telegram-yt-downloader.git```


**2. Створіть та активуйте віртуальне середовище:**
```bash
# Для Windows
python -m venv venv
venv\Scripts\activate
```. 
```bash
# Для macOS/Linux
python3 -m venv venv
source venv/bin/activate
```. 

**3. Встановіть залежності:**
```bash
pip install -r requirements.txt
```. 

**4. Налаштуйте змінні середовища:**
Скопіюйте файл `.env.example` та перейменуйте його на `.env`.
```bash
cp .env.example .env
```. 
Відкрийте файл `.env` та заповніть його своїми даними:
- `TELEGRAM_BOT_TOKEN`: Отримайте у [@BotFather](https://t.me/BotFather).
- `ALLOWED_USER_IDS`: Вкажіть ваш Telegram ID та ID інших довірених користувачів через кому без пробілів.

---

## ⚙️ Використання

### 🤖 Telegram Бот

1.  **Запуск бота:**
    ```bash
    python main_bot.py
    ```
2.  **Робота з ботом:**
    - Надішліть боту посилання на медіа.
    - Оберіть бажану дію за допомогою кнопок (завантажити відео, аудіо або вибрати якість вручну).
    - Користувачі, яких немає у `ALLOWED_USER_IDS`, отримають повідомлення про відмову в доступі.

3.  **Для роботи в групах:**
    - Додайте бота до групи.
    - Перейдіть до [@BotFather](https://t.me/BotFather) -> `/mybots` -> ваш бот -> "Bot Settings" -> "Group Privacy" -> "Turn off". Це дозволить боту бачити посилання у чаті.

### 💻 Консольний Скрипт

1.  **Запуск скрипта:**
    ```bash
    python downloader.py
    ```
2.  **Робота зі скриптом:**
    - Дотримуйтесь інструкцій у меню, яке з'явиться.
    - Введіть посилання, оберіть опцію завантаження (відео/аудіо/вибір формату) та спостерігайте за процесом.
    - Усі файли зберігаються в папку `downloads`.

---

## 📜 Ліцензія

Цей проєкт розповсюджується за ліцензією MIT. Детальніше дивіться у файлі `LICENSE`.
